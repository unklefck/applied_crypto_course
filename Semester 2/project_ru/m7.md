### **Проект: CryptoCore — документ технических требований (Спринт 7)**

**Цель спринта:** Реализовать функции для безопасного получения ключей из паролей и других ключей.

#### **1. Структура проекта и чистота репозитория**

Кодовая база должна быть расширена для поддержки функциональности KDF с сохранением существующей структуры.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| STR-1 | Все требования из предыдущих спринтов (STR-1 ... STR-4) **должны** по-прежнему выполняться.                       | Must      |
| STR-2 | **Должны** быть созданы новые исходные файлы для получения ключей в логичной структуре каталогов.                 | Must      |
|      | - **Рекомендуемый путь:** `src/kdf/`, содержащий `pbkdf2.py`/`pbkdf2.c` и `hkdf.py`/`hkdf.c`                      |           |
| STR-3 | Файл `README.md` **должен** быть обновлён и включать:                                                              | Must      |
|      | - Документацию по новой команде `derive` и её опциям.                                                               |           |
|      | - Объяснение концепций получения ключей (растяжение ключа, salt, число итераций).                                  |           |
|      | - Примеры получения ключей из паролей и построения иерархий ключей.                                                |           |
|      | - Замечания по безопасности (минимальные числа итераций, требования к salt).                                       |           |
| STR-4 | Система сборки **должна** быть обновлена так, чтобы включать любые новые исходные файлы реализаций KDF.           | Must      |

#### **2. Парсер интерфейса командной строки (CLI)**

Для операций получения ключей должна быть реализована новая подкоманда.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| CLI-1 | Инструмент **должен** поддерживать новую подкоманду `derive` для получения ключей.                                | Must      |
| CLI-2 | Подкоманда `derive` **должна** принимать следующие аргументы:                                                      | Must      |
|      | - `--password PASSWORD` : **должен** принимать строку пароля. Если в ней есть специальные символы, shell должен корректно экранировать её. |           |
|      | - `--salt SALT` : **должен** принимать salt как шестнадцатеричную строку. Если salt не указан, инструменту **следует** генерировать случайный 16-байтный salt. |           |
|      | - `--iterations COUNT` : **должен** принимать целое число итераций (по умолчанию: 100000).                        |           |
|      | - `--length LENGTH` : **должен** принимать желаемую длину ключа в байтах (по умолчанию: 32).                      |           |
|      | - `--algorithm ALGORITHM` : **должен** принимать алгоритм KDF (на первом этапе только `pbkdf2`).                  |           |
| CLI-3 | Вывод **должен** печататься в `stdout` в виде `KEY_HEX  SALT_HEX` (оба значения — шестнадцатеричные строки).      | Must      |
| CLI-4 | Инструменту **следует** поддерживать необязательный флаг `--output FILE` для записи полученного ключа в файл.     | Should    |
| CLI-5 | Инструменту **следует** поддерживать чтение пароля из файла или переменной окружения в целях безопасности.         | Could     |

**Примеры вызова:**
```bash
# Базовое получение ключа с явно заданным salt
$ cryptocore derive --password "MySecurePassword123!" --salt a1b2c3d4e5f601234567890123456789 --iterations 100000 --length 32
> 5f4dcc3b5aa765d61d8327deb882cf992b95990a9151374abd7fe5c8f9b8a8c7  a1b2c3d4e5f601234567890123456789

# Получение ключа с автоматически сгенерированным salt
$ cryptocore derive --password "AnotherPassword" --iterations 500000 --length 16
> 8d969eef6ecad3c29a3a629280e686cf0c3f5d5a86aff3ca12020c923adc6c92  e3b0c44298fc1c149afbf4c8996fb924

# Получение ключа с сохранением в файл
$ cryptocore derive --password "app_key" --salt fixedappsalt --iterations 10000 --length 32 --output app.key
```

#### **3. Реализация функций получения ключей**

Необходимо реализовать две функции получения ключей: PBKDF2 с нуля и функцию иерархии ключей.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| KDF-1 | **PBKDF2-HMAC-SHA256** **должен** быть реализован студентом с нуля.                                               | Must      |
|      | - Реализация **должна** следовать спецификации RFC 2898.                                                            |           |
|      | - Она **должна** использовать реализацию HMAC-SHA256 из Спринта 5.                                                 |           |
|      | - Она **должна** эффективно поддерживать число итераций как минимум до 1000000.                                    |           |
| KDF-2 | Реализация PBKDF2 **должна** корректно вычислять:                                                                  | Must      |
|      | `DK = T1 ∥ T2 ∥ ... ∥ Tdklen`, где `Ti = F(P, S, c, i)`, а `F` — это PRF (HMAC-SHA256)                            |           |
|      | `F(P, S, c, i) = U1 ⊕ U2 ⊕ ... ⊕ Uc`, где `U1 = PRF(P, S ∥ INT_32_BE(i))`                                         |           |
|      | `Uj = PRF(P, Uj-1)`                                                                                                 |           |
| KDF-3 | Реализация **должна** поддерживать пароли и salt произвольной длины.                                               | Must      |
| KDF-4 | Реализация **должна** выдавать результат строго той длины, которую запросил пользователь.                          | Must      |
| KDF-5 | **Должна** быть реализована **функция иерархии ключей** для получения нескольких ключей из master key.            | Must      |
|      | - Сигнатура функции: `derive_key(master_key: bytes, context: str, length: int) -> bytes`                           |           |
|      | - Реализация **должна** использовать HMAC детерминированным образом: `HMAC(master_key, context ∥ counter)`       |           |
|      | - Она **должна** поддерживать получение ключей произвольной длины (генерируя достаточное число байтов и обрезая результат). |           |
|      | - Строка `context` **должна** быть уникальным идентификатором назначения ключа (например, `"encryption"`, `"authentication"`). |           |

**Ожидаемая структура реализации PBKDF2:**
```python
# Python: src/kdf/pbkdf2.py
import hashlib
import hmac

def pbkdf2_hmac_sha256(password, salt, iterations, dklen):
    """PBKDF2-HMAC-SHA256 implementation"""
    # Password and salt should be bytes
    if isinstance(password, str):
        password = password.encode('utf-8')
    if isinstance(salt, str):
        salt = bytes.fromhex(salt) if all(c in '0123456789abcdefABCDEF' for c in salt) else salt.encode('utf-8')

    # Calculate number of blocks needed
    blocks_needed = (dklen + 31) // 32  # SHA-256 produces 32-byte output
    derived_key = b''

    for i in range(1, blocks_needed + 1):
        # Compute U1 = HMAC(password, salt || INT_32_BE(i))
        block = hmac_sha256(password, salt + i.to_bytes(4, 'big'))
        u_prev = block

        # Compute U2 through Uc
        for j in range(2, iterations + 1):
            u_curr = hmac_sha256(password, u_prev)
            # XOR u_curr into block
            block = bytes(a ^ b for a, b in zip(block, u_curr))
            u_prev = u_curr

        derived_key += block

    # Return exactly dklen bytes
    return derived_key[:dklen]

def hmac_sha256(key, msg):
    """HMAC-SHA256 using our implementation from Sprint 5"""
    # Use HMAC class from src/mac/hmac.py
    pass
```

**Ожидаемая реализация иерархии ключей:**
```python
# Python: src/kdf/hkdf.py (упрощённая версия для иерархии ключей)
def derive_key(master_key, context, length=32):
    """Derive a key from a master key using a deterministic HMAC-based method"""
    if isinstance(context, str):
        context = context.encode('utf-8')

    derived = b''
    counter = 1

    while len(derived) < length:
        # T_i = HMAC(master_key, context || counter)
        block = hmac_sha256(master_key, context + counter.to_bytes(4, 'big'))
        derived += block
        counter += 1

    return derived[:length]
```

#### **4. Файловый ввод-вывод для получения ключей**

Функциональность получения ключей должна безопасно обрабатывать ввод пароля и salt.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| IO-1 | Пароль **должен** читаться из аргумента командной строки или, если это реализовано, из файла либо переменной окружения. | Must |
| IO-2 | Salt **должен** читаться как шестнадцатеричная строка или генерироваться случайно, если он не задан.              | Must      |
| IO-3 | Если указан `--output`, полученный ключ **должен** записываться в файл как сырые бинарные байты, а не как шестнадцатеричная строка. | Must |
|      | - Salt **не следует** записывать в файл ключа, если это не запрошено явно.                                         |           |
| IO-4 | Инструменту **следует** очищать пароль из памяти как можно раньше после использования.                              | Should    |

#### **5. Тестирование и верификация**

Полное тестирование должно гарантировать корректность и совместимость со стандартными реализациями.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| TEST-1 | **Тесты с известным ответом:** реализация PBKDF2 **должна** проходить тестовые векторы из RFC 6070.              | Must      |
|      | - В частности, тестовые векторы для PBKDF2 с HMAC-SHA256.                                                          |           |
| TEST-2 | **Тест итераций:** реализация **должна** выдавать один и тот же результат при многократном запуске с одинаковыми параметрами. | Must |
| TEST-3 | **Тест длины:** реализация **должна** корректно получать ключи различной длины (1-100 байт).                     | Must      |
| TEST-4 | **Тест совместимости:** вывод PBKDF2 **должен** совпадать с реализацией OpenSSL:                                  | Must      |
|      | `openssl kdf -keylen 32 -kdfopt pass:PASSWORD -kdfopt salt:SALT -kdfopt iter:ITERATIONS PBKDF2`                  |           |
| TEST-5 | **Тест иерархии ключей:** функция `derive_key` **должна** выдавать детерминированный результат для одинаковых входных данных. | Must |
| TEST-6 | **Тест разделения контекстов:** разные строки контекста **должны** порождать полностью разные ключи из одного и того же master key. | Must |
| TEST-7 | **Тест случайности salt:** если salt не передан, сгенерированные salt **должны** быть уникальными и случайными (1000 генераций без дубликатов). | Must |
| TEST-8 | **Тест производительности:** студенту **следует** измерить и задокументировать время работы для разных чисел итераций (10k, 100k, 1M). | Should |

**Примеры тестовых команд:**
```bash
# Тест по вектору RFC 6070 №1
$ cryptocore derive --password "password" --salt 73616c74 --iterations 1 --length 20
> Should produce: 0c60c80f961f0e71f3a9b524af6012062fe037a6

# Тест по вектору RFC 6070 №2
$ cryptocore derive --password "password" --salt 73616c74 --iterations 2 --length 20
> Should produce: ea6c014dc72d6f8ccd1ed92ace1d41f0d8de8957

# Тест совместимости с OpenSSL
$ cryptocore derive --password "test" --salt 1234567890abcdef --iterations 1000 --length 32 > my_output.txt
$ openssl kdf -keylen 32 -kdfopt pass:test -kdfopt salt:1234567890abcdef -kdfopt iter:1000 PBKDF2 > openssl_output.txt
$ diff my_output.txt openssl_output.txt

# Тест функции иерархии ключей
$ python3 -c "
from src.kdf.hkdf import derive_key
master = b'0' * 32
key1 = derive_key(master, 'encryption', 32)
key2 = derive_key(master, 'authentication', 32)
print(f'Key1: {key1.hex()}')
print(f'Key2: {key2.hex()}')
print(f'Different: {key1 != key2}')
"
```

**Пример тестового сценария для векторов RFC 6070:**
```python
# tests/test_pbkdf2_vectors.py
from src.kdf.pbkdf2 import pbkdf2_hmac_sha256

def test_rfc_6070():
    """Test PBKDF2 with RFC 6070 test vectors"""
    test_cases = [
        {
            'password': b'password',
            'salt': b'salt',
            'iterations': 1,
            'dklen': 20,
            'expected': '0c60c80f961f0e71f3a9b524af6012062fe037a6'
        },
        {
            'password': b'password',
            'salt': b'salt',
            'iterations': 2,
            'dklen': 20,
            'expected': 'ea6c014dc72d6f8ccd1ed92ace1d41f0d8de8957'
        },
        {
            'password': b'password',
            'salt': b'salt',
            'iterations': 4096,
            'dklen': 20,
            'expected': '4b007901b765489abead49d926f721d065a429c1'
        },
        {
            'password': b'passwordPASSWORDpassword',
            'salt': b'saltSALTsaltSALTsaltSALTsaltSALTsalt',
            'iterations': 4096,
            'dklen': 25,
            'expected': '3d2eec4fe41c849b80c8d83662c0e44a8b291a964cf2f07038'
        }
    ]

    for i, test in enumerate(test_cases):
        result = pbkdf2_hmac_sha256(
            test['password'],
            test['salt'],
            test['iterations'],
            test['dklen']
        )
        expected_bytes = bytes.fromhex(test['expected'])

        assert result == expected_bytes, f"RFC 6070 test case {i+1} failed"
        print(f"✓ RFC 6070 test case {i+1} passed")

    print("All RFC 6070 test vectors passed")
```
