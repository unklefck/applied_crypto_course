### **Проект: CryptoCore — документ технических требований (Спринт 6)**

**Цель спринта:** Объединить шифрование и аутентификацию в одну безопасную операцию.

#### **1. Структура проекта и чистота репозитория**

Кодовая база должна быть расширена для поддержки режимов аутентифицированного шифрования с сохранением существующей структуры.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| STR-1 | Все требования из предыдущих спринтов (STR-1 ... STR-4) **должны** по-прежнему выполняться.                       | Must      |
| STR-2 | **Должны** быть созданы новые исходные файлы для аутентифицированного шифрования.                                  | Must      |
|      | - **Рекомендуемый путь:** `src/modes/gcm.py`/`gcm.c` для реализации GCM                                             |           |
|      | - **Рекомендуемый путь:** `src/aead/` для общих интерфейсов AEAD и схемы Encrypt-then-MAC                         |           |
| STR-3 | Файл `README.md` **должен** быть обновлён и включать:                                                              | Must      |
|      | - Документацию по новому режиму GCM и функциональности AAD.                                                         |           |
|      | - Объяснение концепций AEAD и их свойств безопасности.                                                              |           |
|      | - Примеры шифрования и расшифрования с associated data.                                                             |           |
|      | - Предупреждения о катастрофическом характере отказа при неверном AAD или подменённых данных.                      |           |
| STR-4 | Система сборки **должна** быть обновлена с учётом любых новых исходных файлов для реализаций AEAD.                | Must      |

#### **2. Парсер интерфейса командной строки (CLI)**

CLI для шифрования/расшифрования должен быть расширен для поддержки режима GCM и associated data.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| CLI-1 | Аргумент `--mode` **должен** быть расширен и принимать значение `gcm`.                                             | Must      |
| CLI-2 | Для режимов аутентифицированного шифрования **должен** быть добавлен новый аргумент `--aad DATA`.                 | Must      |
|      | - AAD **должен** передаваться как шестнадцатеричная строка.                                                        |           |
|      | - AAD **должен** быть необязательным; если не передан, он должен считаться пустым.                                |           |
| CLI-3 | Для режима GCM при шифровании инструмент **должен** генерировать случайный `nonce` длиной 12 байт (вместо 16-байтного IV). | Must |
|      | - `nonce` **должен** записываться в начало выходного шифртекста, аналогично работе с IV в предыдущих режимах.    |           |
| CLI-4 | При расшифровании GCM `nonce` **должен** считываться из входного файла (первые 12 байт) либо передаваться через `--iv` (для согласованности его следует переименовать в `--nonce`, но сохранение `--iv` ради обратной совместимости допустимо). | Must |
| CLI-5 | При расшифровании вывод **должен** подавляться, если аутентификация не прошла. Инструмент **должен** завершаться с ошибкой и не записывать расшифрованные данные в выходной файл. | Must |

**Примеры вызова:**
```bash
# GCM-шифрование с AAD
$ cryptocore --algorithm aes --mode gcm --encrypt --key 00112233445566778899aabbccddeeff --input plaintext.txt --output ciphertext.bin --aad aabbccddeeff

# GCM-расшифрование с корректным AAD
$ cryptocore --algorithm aes --mode gcm --decrypt --key 00112233445566778899aabbccddeeff --input ciphertext.bin --output decrypted.txt --aad aabbccddeeff
> [SUCCESS] Decryption completed successfully

# GCM-расшифрование с неверным AAD
$ cryptocore --algorithm aes --mode gcm --decrypt --key 00112233445566778899aabbccddeeff --input ciphertext.bin --output decrypted.txt --aad wrongaad123
> [ERROR] Authentication failed: AAD mismatch or ciphertext tampered
> Exit code: 1
```

#### **3. Реализация аутентифицированного шифрования**

В этом спринте нужно реализовать как общую схему Encrypt-then-MAC, так и конкретный режим GCM.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| AEAD-1 | Парадигма Encrypt-then-MAC **должна** быть реализована как составная функция.                                    | Must      |
|      | - Она должна комбинировать любой режим блочного шифра (например, CTR) с HMAC из Спринта 5.                        |           |
|      | - Реализация **должна** следовать формуле: `C = E(K_e, P), T = MAC(K_m, C ∥ AAD), output = C ∥ T`                |           |
|      | - Для шифрования и MAC **должны** использоваться разные ключи (полученные из master key через KDF либо через разделение ключевого материала). |           |
| AEAD-2 | Режим GCM **должен** быть реализован с нуля.                                                                      | Must      |
|      | - Реализация **должна** следовать спецификации NIST SP 800-38D.                                                    |           |
|      | - Для шифрования она **должна** использовать примитив AES из предыдущих спринтов в режиме CTR.                    |           |
| AEAD-3 | Реализация GCM **должна** включать умножение в поле Галуа `GF(2^128)`.                                            | Must      |
|      | - Представление поля **должно** использовать неприводимый многочлен `x^128 + x^7 + x^2 + x + 1`.                 |           |
|      | - Реализация **должна** быть эффективной (рекомендуется использовать предвычисленные таблицы или алгоритм Карацубы). |           |
| AEAD-4 | GCM **должен** корректно обрабатывать:                                                                            | Must      |
|      | - `nonce` (рекомендуется 12 байт, но для других длин должна поддерживаться обработка через GHASH)                 |           |
|      | - Associated Data (AAD) произвольной длины                                                                          |           |
|      | - Открытый текст произвольной длины                                                                                 |           |
| AEAD-5 | Аутентификационный тег **должен** иметь длину 16 байт (128 бит) и добавляться в конец шифртекста.                | Must      |
|      | - Формат вывода: `Nonce (12 bytes) ∥ Ciphertext ∥ Tag (16 bytes)`                                                 |           |
| AEAD-6 | При расшифровании реализация **должна** проверять тег до вывода любого открытого текста.                         | Must      |
|      | - Если проверка не прошла, никакой открытый текст не должен выводиться, и ошибка должна возникать немедленно.    |           |

**Ожидаемая структура реализации GCM:**
```python
# Python: src/modes/gcm.py
class GCM:
    def __init__(self, key, nonce=None):
        self.aes = AES(key) # AES implementation from earlier sprints
        self.nonce = nonce or os.urandom(12)
        # Precompute multiplication table for GHASH
        self._precompute_table()

    def _ghash(self, aad, ciphertext):
        """Compute GHASH in GF(2^128)"""
        # Implementation of Galois Field multiplication
        pass

    def _mult_gf(self, x, y):
        """Multiply two elements in GF(2^128) modulo x^128 + x^7 + x^2 + x + 1"""
        pass

    def encrypt(self, plaintext, aad=b""):
        # Generate J0 from nonce
        # Encrypt using CTR mode with J0+1, J0+2, ...
        # Compute authentication tag
        # Return nonce + ciphertext + tag
        pass

    def decrypt(self, data, aad=b""):
        # Extract nonce, ciphertext, tag
        # Verify authentication tag
        # If verification fails, raise exception immediately
        # If verification succeeds, decrypt using CTR mode
        pass
```

**Пример умножения в поле Галуа:**
```python
def _mult_gf(self, x, y):
    """GF(2^128) multiplication using the irreducible polynomial"""
    z = 0
    v = y
    for i in range(127, -1, -1):
        if (x >> i) & 1:
            z ^= v
        if v & 1:
            v = (v >> 1) ^ 0xE1000000000000000000000000000000
        else:
            v >>= 1
    return z
```

#### **4. Файловый ввод-вывод для AEAD**

Логика работы с файлами должна быть расширена для поддержки новых AEAD-форматов и требований безопасности.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| IO-1 | Для GCM-шифрования выходной файл **должен** содержать: `12-byte nonce ∥ ciphertext ∥ 16-byte tag`.               | Must      |
| IO-2 | Для GCM-расшифрования входной файл **должен** разбираться как: `12-byte nonce ∥ ciphertext ∥ 16-byte tag`.       | Must      |
| IO-3 | Если аутентификация при расшифровании не прошла, инструмент **не должен** создавать или записывать выходной файл. | Must      |
|      | - Если выходной файл был создан частично, инструмент **должен** удалить его при отказе аутентификации.            |           |
| IO-4 | AAD **должен** передаваться в функции шифрования/расшифрования как бинарная строка.                                | Must      |

#### **5. Тестирование и верификация**

Полное тестирование должно проверять и корректность, и критически важное свойство отказа при провале аутентификации.

| ID  | Описание требования                                                                                                 | Приоритет |
| :-- | :------------------------------------------------------------------------------------------------------------------ | :-------- |
| TEST-1 | **Тесты с известным ответом:** реализация GCM **должна** проходить тестовые векторы из NIST SP 800-38D.         | Must      |
|      | - В частности, векторы с разной длиной AAD и разной длиной сообщения.                                              |           |
| TEST-2 | **Тест полного цикла:** шифрование и последующее расшифрование с тем же AAD **должны** возвращать исходный открытый текст. | Must |
| TEST-3 | **Тест подмены AAD:** расшифрование с неверным AAD **должно** завершаться катастрофическим отказом.              | Must      |
|      | - Инструмент **не должен** выводить никаких расшифрованных данных.                                                 |           |
|      | - Инструмент **должен** возвращать ненулевой код завершения.                                                       |           |
|      | - Инструмент **должен** печатать понятное сообщение об ошибке аутентификации.                                      |           |
| TEST-4 | **Тест подмены шифртекста:** изменение любого бита в шифртексте или теге **должно** приводить к провалу аутентификации. | Must |
|      | - Отказ **должен** быть катастрофическим (без вывода открытого текста).                                            |           |
| TEST-5 | **Тест повторного использования nonce:** реализация **должна** генерировать случайный nonce для каждого шифрования. | Must |
|      | - При генерации 1000 `nonce` должны получаться 1000 уникальных значений.                                           |           |
| TEST-6 | **Тест пустого AAD:** реализация **должна** корректно работать с пустым AAD.                                      | Must      |
| TEST-7 | **Тест большого AAD:** реализация **должна** уметь обрабатывать AAD размером больше доступной памяти, работая по частям. | Must |
| TEST-8 | **Тест совместимости:** реализацию GCM **следует** проверить на совместимость с OpenSSL:                          | Should    |
|      | `openssl enc -aes-128-gcm -K <key> -iv <nonce> -aad <aad> -in <file>`                                              |           |
| TEST-9 | **Тест Encrypt-then-MAC:** составная реализация Encrypt-then-MAC **должна** проходить аналогичные тесты аутентификации. | Must |

**Примеры тестовых команд:**
```bash
# Тест с вектором NIST GCM
$ echo -n "Hello GCM world" > test_input.txt
$ cryptocore --algorithm aes --mode gcm --encrypt --key 00000000000000000000000000000000 --iv 000000000000000000000000 --aad "" --input test_input.txt --output test_output.bin

# Обнаружение подмены: изменить шифртекст
$ dd if=test_output.bin of=tampered.bin bs=1 count=100 conv=notrunc
$ echo "XX" | dd of=tampered.bin bs=1 seek=50 count=2 conv=notrunc
$ cryptocore --algorithm aes --mode gcm --decrypt --key 00000000000000000000000000000000 --input tampered.bin --output should_fail.txt --aad ""
> [ERROR] Authentication failed: ciphertext tampered
> Exit code: 1

# Проверить, что выходной файл не создан
$ ls should_fail.txt
> ls: cannot access 'should_fail.txt': No such file or directory
```

**Пример тестового сценария для свойств безопасности GCM:**
```python
# tests/test_gcm_security.py
import os
from src.modes.gcm import GCM

def test_gcm_aad_tamper():
    """Test that wrong AAD causes catastrophic failure"""
    key = os.urandom(16)
    plaintext = b"Secret message"
    aad_correct = b"correct_aad"
    aad_wrong = b"wrong_aad"
    # Encrypt with correct AAD
    gcm = GCM(key)
    ciphertext = gcm.encrypt(plaintext, aad_correct)
    # Try to decrypt with wrong AAD
    gcm2 = GCM(key, gcm.nonce)
    try:
        result = gcm2.decrypt(ciphertext, aad_wrong)
        assert False, "Decryption should have failed with wrong AAD"
    except AuthenticationError:
        print("✓ Correctly failed with wrong AAD")
    # Verify no plaintext was returned
    assert 'result' not in locals() or result is None

def test_gcm_ciphertext_tamper():
    """Test that ciphertext tampering causes catastrophic failure"""
    key = os.urandom(16)
    plaintext = b"Another secret message"
    aad = b"associated_data"
    # Encrypt
    gcm = GCM(key)
    ciphertext = gcm.encrypt(plaintext, aad)
    # Tamper with ciphertext (flip one bit)
    tampered = bytearray(ciphertext)
    tampered[20] ^= 0x01 # Flip one bit in the ciphertext
    # Try to decrypt tampered ciphertext
    gcm2 = GCM(key, gcm.nonce)
    try:
        result = gcm2.decrypt(bytes(tampered), aad)
        assert False, "Decryption should have failed with tampered ciphertext"
    except AuthenticationError:
        print("✓ Correctly failed with tampered ciphertext")
```
